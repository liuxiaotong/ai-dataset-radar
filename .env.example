# AI Dataset Radar - Environment Variables
# Copy this file to .env and fill in your values
# python-dotenv will auto-load this file on startup

# ── LLM Insights (optional) ──────────────────────────────
# Enables automatic competitive intelligence analysis after each scan.
# Without this key, the scan still runs but only saves a prompt file
# for manual analysis (e.g. via Claude Code).
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxx

# ── LLM Provider (optional) ──────────────────────────────
# Default: uses Anthropic (ANTHROPIC_API_KEY above).
# Set LLM_PROVIDER=openai_compatible for Kimi, DeepSeek, Qwen, Zhipu, OpenAI:
# LLM_PROVIDER=openai_compatible
# LLM_API_KEY=sk-xxx
# LLM_BASE_URL=https://api.moonshot.cn/v1
# LLM_MODEL=moonshot-v1-128k
#
# Presets:
#   Kimi:     LLM_BASE_URL=https://api.moonshot.cn/v1       LLM_MODEL=moonshot-v1-128k
#   DeepSeek: LLM_BASE_URL=https://api.deepseek.com         LLM_MODEL=deepseek-chat
#   Qwen:     LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1  LLM_MODEL=qwen-plus
#   Zhipu:    LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/  LLM_MODEL=glm-4-plus

# ── GitHub API (recommended) ─────────────────────────────
# Without a token: 60 requests/hour (may miss data for 15+ orgs).
# With a token:  5,000 requests/hour.
# Create at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=ghp_xxx

# ── REST API Authentication (optional) ───────────────────
# If set, all /scan requests require X-API-Key header or ?api_key= param.
# If unset, the API runs without authentication (fine for local use).
RADAR_API_KEY=

# ── REST API Rate Limit (optional) ───────────────────────
# Max requests per minute per IP. Default: 60.
# RADAR_RATE_LIMIT=60

# ── Alert Notifications (optional) ──────────────────────
# Configure to receive alerts when anomalies are detected.
# SMTP_SERVER=smtp.gmail.com
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=radar@yourcompany.com
# EMAIL_TO=team@yourcompany.com
# WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
